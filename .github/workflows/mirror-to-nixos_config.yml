name: Mirror to nixos_config (no Actions)
on:
  push:
    branches: [ master ]     # add more branches if you want mirrored
  workflow_dispatch: {}     # manual trigger if needed

permissions:
  contents: read

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.NIXOS_CONFIG_DEPLOY_KEY }}

      - name: Trust github.com host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Configure git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Push to nixos_config (preserving downstream files)
        run: |
          # Save current commit before removing workflows
          AXIOS_COMMIT=$(git rev-parse HEAD)
          
          # Remove .github/workflows 
          git rm -rf .github/workflows
          git commit -m "Remove .github/workflows for downstream"
          
          git remote add target git@github.com:kcalvelli/nixos_config.git
          git fetch target
          
          # Checkout target branch and merge our changes into it
          git checkout -b sync-branch target/master
          git merge --allow-unrelated-histories -X theirs HEAD -m "Sync from axios (preserving downstream files)"
          git push target sync-branch:master
